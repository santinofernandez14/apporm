# app orm con typescript

## Instalacion

```shell
npm install

```

## Ejecutar

```shell
crear la carpeta build antes
crear el .env basandose en el ejemplo y completarlo con los datos del ejemplo
npm run build
npm start

```